import { STEP_DELAY_MS } from "./feature-time";
import { MINIMUM_TOKENS } from "./feature-tokens";
import { SCREEN_WIDTH } from "./feature-pixels";
import { CACHE_INTERVAL_MS } from "./feature-cache";
import { RECONNECT_DELAY_MS } from "./feature-reconnect";
import { POLL_INTERVAL_MS } from "./feature-poll";

console.log(STEP_DELAY_MS, MINIMUM_TOKENS, SCREEN_WIDTH);
console.log(CACHE_INTERVAL_MS, RECONNECT_DELAY_MS, POLL_INTERVAL_MS);
